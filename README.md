When conduct translation, use the followig prompts:

generate a task list for the documents in @docs/tutorials/projects/ based on the workflow in QWEN.md and run the task list, you should always git diff to determine the changes and update the translation accordingly. 