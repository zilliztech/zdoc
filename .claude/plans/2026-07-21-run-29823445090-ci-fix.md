# Run 29823445090 CI Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the Python SDK generation, Java fallback reconciliation, and Guides translation validation failures from GitHub Actions run 29823445090.

**Architecture:** Keep Drive fallback sources authoritative by rebuilding a target from the complete merged Drive source tree whenever an incremental current-version render also consumes a fallback chain. Resolve duplicate Drive source aliases deterministically only when the canonical `<token>.json` file exists, retaining fail-closed behavior for genuinely ambiguous aliases. Give npm validation separate private user and global config files so npm does not reject the isolated environment.

**Tech Stack:** Node.js 20, Docusaurus plugin code, `node:test`, GitHub Actions workflow helpers.

---

### Task 1: Rebuild SDK output when incremental Drive rendering consumes fallbacks

**Files:**
- Modify: `plugins/lark-docs/larkDocScraper.test.js`
- Modify: `plugins/lark-docs/index.js`

- [ ] **Step 1: Write the failing integration test**

Add `testDriveFallbackIncrementalRebuildsCompleteOutput()` beside the existing Drive incremental tests. Stub an incremental plan with no expanded tokens, configure a real `fallbackSourceDir`, and assert that the command cleans the generated target, calls `writer.write_docs(outputDir, rootToken)`, regenerates the sidebar even with an empty current-manual delta, and post-processes the rebuilt output. Assert that `write_subtree()` is not used.

- [ ] **Step 2: Run the test to verify it fails**

Run: `node plugins/lark-docs/larkDocScraper.test.js`

Expected: FAIL because the current incremental branch performs no render when `expanded_tokens` is empty.

- [ ] **Step 3: Implement the minimal authoritative fallback render**

In `plugins/lark-docs/index.js`, derive a boolean for an incremental Drive plan with `fallbackSourceDir`. For that case, call `pre_process_file_paths()` with the existing preserved-file list and render the complete root with `writer.write_docs()`. Include this condition in sidebar regeneration and post-processing decisions. Preserve the existing token-scoped incremental path for Drive manuals without fallbacks and for Wiki manuals.

- [ ] **Step 4: Run the focused test to verify it passes**

Run: `node plugins/lark-docs/larkDocScraper.test.js`

Expected: PASS with `lark-docs scraper tests passed`.

### Task 2: Prefer canonical Drive source files over alias duplicates

**Files:**
- Modify: `plugins/lark-docs/larkUtils.test.js`
- Modify: `plugins/lark-docs/larkUtils.js`

- [ ] **Step 1: Write the failing fallback reconciliation test**

Add a fixture where an alias filename and `<token>.json` both deserialize to the same touched folder token, while the canonical file provides the reconciled placement. Assert that fallback reconciliation succeeds. Retain the existing test proving two noncanonical duplicate files still throw a duplicate-token error.

- [ ] **Step 2: Run the test to verify it fails**

Run: `node plugins/lark-docs/larkUtils.test.js`

Expected: FAIL with `[fallback-source] Duplicate token` for the alias plus canonical pair.

- [ ] **Step 3: Implement canonical-file selection**

In `resolveSource()`, after parent filtering and before the ambiguity error, select the single candidate whose filename exactly equals `${token}.json`. Continue throwing when multiple candidates remain and no canonical filename exists.

- [ ] **Step 4: Run the focused test to verify it passes**

Run: `node plugins/lark-docs/larkUtils.test.js`

Expected: PASS with `larkUtils tests passed`.

### Task 3: Separate npm user and global validation configs

**Files:**
- Modify: `scripts/docs-workflow/validate-guides-translation-staging.test.js`
- Modify: `scripts/docs-workflow/validate-guides-translation-staging.js`

- [ ] **Step 1: Strengthen the hostile-environment test**

Save and poison both `NPM_CONFIG_USERCONFIG` and `NPM_CONFIG_GLOBALCONFIG`. In the executor assertion, require both paths to be private empty files and require the paths to differ.

- [ ] **Step 2: Run the test to verify it fails**

Run: `node --test scripts/docs-workflow/validate-guides-translation-staging.test.js`

Expected: FAIL because both variables currently point to the same `npmrc` file.

- [ ] **Step 3: Create distinct isolated npm configs**

Create `user-npmrc` and `global-npmrc` inside the existing mode-0700 isolation root, write each as an empty file, and assign them separately to `NPM_CONFIG_USERCONFIG` and `NPM_CONFIG_GLOBALCONFIG`.

- [ ] **Step 4: Run the focused test to verify it passes**

Run: `node --test scripts/docs-workflow/validate-guides-translation-staging.test.js`

Expected: all tests pass.

### Task 4: Verify the combined repair and downstream compatibility

**Files:**
- Verify only.

- [ ] **Step 1: Run all directly affected tests**

Run: `node --test plugins/lark-docs/larkUtils.test.js scripts/docs-workflow/validate-guides-translation-staging.test.js scripts/validate-generated-sidebars.test.js && node plugins/lark-docs/larkDocScraper.test.js`

Expected: all tests pass.

- [ ] **Step 2: Run workflow policy and SDK workflow tests**

Run: `node --test scripts/validate-workflow-policy.test.js scripts/sdk-reference-workflow.test.js scripts/docs-workflow/run-content-group.test.js`

Expected: all tests pass.

- [ ] **Step 3: Validate generated sidebars and the repository build**

Run: `node scripts/validate-generated-sidebars.js && pnpm run build`

Expected: sidebar validation and Docusaurus build exit 0.

- [ ] **Step 4: Assess downstream `zdoc_cn` compatibility**

Confirm the changed files are tooling and tests only, with no generated schema or content contract changes consumed by `../zdoc_cn`. If the build or shared workflow contract changes unexpectedly, run the corresponding downstream tests before completion.
