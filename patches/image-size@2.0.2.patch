diff --git a/dist/detector.cjs b/dist/detector.cjs
index 0898608dbeca36722dfd795341b1c3c1448f58aa..a5febc1a63b46211b692c08c32a3f418120252d6 100644
--- a/dist/detector.cjs
+++ b/dist/detector.cjs
@@ -160,7 +160,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -251,6 +251,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
@@ -484,7 +485,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/detector.mjs b/dist/detector.mjs
index 67afcab02d627f95e98c938499e07a934b832a26..658b0236832d9a30c8f165603129a005f630c16a 100644
--- a/dist/detector.mjs
+++ b/dist/detector.mjs
@@ -158,7 +158,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,6 +249,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
@@ -482,7 +483,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/fromFile.cjs b/dist/fromFile.cjs
index c226d3dac07f235db456c774b26cca88f655e1af..11b97e19ee886c250bda9a790f828545afd77d84 100644
--- a/dist/fromFile.cjs
+++ b/dist/fromFile.cjs
@@ -186,7 +186,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -277,6 +277,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -510,7 +511,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/fromFile.mjs b/dist/fromFile.mjs
index 4a3c44432982397204f7a9d04fc66ef65a8e7ba4..e8670458c0b65b97c5792e77df6ac1130c40967f 100644
--- a/dist/fromFile.mjs
+++ b/dist/fromFile.mjs
@@ -163,7 +163,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -254,6 +254,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -487,7 +488,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210eb131db06ccca30a5c466c7252a03af50..7ef13e4e098d104d58d862f3cd758fc21d20a588 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -162,7 +162,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -253,6 +253,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -486,7 +487,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17afad4e406eba013fff7858366cb5f8e3ba4..b86e96153982643e866e686c52e213bda6f7534f 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -158,7 +158,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,6 +249,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -482,7 +483,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/lookup.cjs b/dist/lookup.cjs
index 9731ad8bb0949f8fdf9297851bdb312e91282f01..5caea26b1141790227609146d8f64c51a3eff3bc 100644
--- a/dist/lookup.cjs
+++ b/dist/lookup.cjs
@@ -160,7 +160,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -251,6 +251,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -484,7 +485,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/lookup.mjs b/dist/lookup.mjs
index f787d1c8408d7b681fec8617595d058f66959869..184371ac257a89bc0f64ff09b652befcc912ecd2 100644
--- a/dist/lookup.mjs
+++ b/dist/lookup.mjs
@@ -158,7 +158,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,6 +249,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
       imageOffset += imageHeader[1];
@@ -482,7 +483,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f490788ea654ea9896060a5c0253d81e348af92..d20fd0bd07b72d07c6f7c62f945290bc7960e62a 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -58,7 +58,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 01330919e01b3c034a96a7b4b1dbf5a7f658bb2d..7e959def058ec6a9590e979053d0549c32f91f0d 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -56,7 +56,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d370dcf80a10f14a6deebfdbe04d3c70585c..fcff141e26a32d527e8e3489858aee251705887c 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -72,6 +72,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027bc5890a79d05a7fdb87de3018e556729e..f043649c0b6bcc4772535a540939c03ec611a85e 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -70,6 +70,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
diff --git a/dist/types/index.cjs b/dist/types/index.cjs
index 6949cd134db7ab59dae7b804b36c7bacb4e387cf..8e87766af6d372bbc95491d85cbdd73c36992ec6 100644
--- a/dist/types/index.cjs
+++ b/dist/types/index.cjs
@@ -160,7 +160,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -251,6 +251,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
@@ -484,7 +485,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/index.mjs b/dist/types/index.mjs
index b026dda4aa16c6077a76e4b3cfe3f54fe1d6d1d0..02004f288b9454d0189171ff6a81215c9cf1c143 100644
--- a/dist/types/index.mjs
+++ b/dist/types/index.mjs
@@ -158,7 +158,7 @@ var HEIF = {
     let currentOffset = ipcoBox.offset + 8;
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
-      if (!ispeBox) break;
+      if (!ispeBox || ispeBox.size <= 0) break;
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,6 +249,7 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      if (imageHeader[1] <= 0) break;
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
       imageOffset += imageHeader[1];
@@ -482,7 +483,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095be85c7a8fd25763895a47c46b4292ebd9c..09c07782285ec8ffe2eb683a1403f2bfafe10471 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -115,7 +115,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4cac6abb366378e5feed745f16981b441c0..6cc22f2b18c5a7584fe423a265fbb5d1f4768f0c 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -113,7 +113,7 @@ function extractPartialStreams(input) {
   let offset = 0;
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
-    if (!jxlpBox) break;
+    if (!jxlpBox || jxlpBox.size <= 0) break;
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
