---
title: "IndexType | Python | MilvusClient"
slug: /python/python/Collections-IndexType
sidebar_label: "IndexType"
beta: false
added_since: Inherit
last_modified: v2.6.x
deprecate_since: false
notebook: false
description: "以下の定数を提供する列挙型です。 | Python | MilvusClient"
type: docx
token: SlrCd1GEbooMh2xLylZc6Oirnrc
sidebar_position: 15
keywords: 
  - NLP
  - Neural Network
  - Deep Learning
  - ナレッジベース
  - zilliz
  - zilliz cloud
  - cloud
  - IndexType
  - pymilvus30
displayed_sidebar: pythonSidebar

displayed_sidbar: pythonSidebar
---

import Admonition from '@theme/Admonition';


# IndexType

以下の定数を提供する列挙型です。

## Constants\{#constants}

- GPU_BRUTE_FORCE

    インデックスタイプを **GPU_BRUTE_FORCE** に設定します。これは GPU インデックスにのみ適用されます。

- GPU_IVF_FLAT

    インデックスタイプを **GPU_IVF_FLAT** に設定します。これは GPU インデックスにのみ適用されます。

- GPU_IVF_PQ

    インデックスタイプを **GPU_IVF_PQ** に設定します。これは GPU インデックスにのみ適用されます。

- GPU_CAGRA

    インデックスタイプを **GPU_CAGRA** に設定します。これは GPU インデックスにのみ適用されます。

- FLAT

    インデックスタイプを **FLAT** に設定します。

- IVF_FLAT

    インデックスタイプを **IVF_FLAT** に設定します。

- IVF_PQ

    インデックスタイプを **IVF_PQ** に設定します。

- SCANN

    インデックスタイプを **SCANN** に設定します。

- IVF_SQ8

    インデックスタイプを **IVF_SQ8** に設定します。

- IVF_RABITQ

    インデックスタイプを **IVF_RABITQ** に設定します。

- BIN_FLAT

    インデックスタイプを **BIN_FLAT** に設定します。これはバイナリベクトルにのみ適用されます。

- BIN_IVF_FLAT

    インデックスタイプを **BIN_IVF_FLAT** に設定します。これはバイナリベクトルにのみ適用されます。

- HNSW

    インデックスタイプを **HNSW** に設定します。

- HNSW_SQ

    インデックスタイプを **HNSW_SQ** に設定します。

- HNSW_PQ

    インデックスタイプを **HNSW_PQ** に設定します。

- HNSW_PRQ

    インデックスタイプを **HNSW_PRQ** に設定します。

- DISKANN

    インデックスタイプを **DISKANN** に設定します。

- SPARSE_INVERTED_INDEX

    インデックスタイプを **SPARSE_INVERTED_INDEX** に設定します。これはスパースベクトルにのみ適用されます。

- SPARSE_WAND

    インデックスタイプを **SPARSE_WAND** に設定します。これはスパースベクトルにのみ適用されます。

- INVERTED

    インデックスタイプを **INVERTED** に設定します。これはスカラーフィールドにのみ適用されます。

- STL_SORT

    インデックスタイプを **STL_SORT** に設定します。これはスカラーフィールドにのみ適用されます。

- TRIE / Trie

    インデックスタイプを **TRIE** に設定します。これは VarChar スカラーフィールドにのみ適用されます。

- AUTOINDEX

    インデックスタイプを **AUTOINDEX** に設定します。
